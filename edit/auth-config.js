// Use "html" for the built-in login screen.
// After server-side Basic authentication is enabled for /edit/, change this to "server" to avoid a second login prompt.
window.VENUE_EDIT_AUTH_MODE = "html";
